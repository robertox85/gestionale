{% extends 'base.html.twig' %}

{% block content %}


    <h2 class="mb-4 text-xl font-bold text-gray-900 dark:text-white">
        {{ 'Add new group'|trans }}
    </h2>

    <form action="{{ url('gruppi/crea') }}" method="POST">
        <input type="hidden" name="csrf_token" value="{{ csrf_token('gruppi') }}"/>


        <div class="grid gap-4 sm:grid-cols-2 sm:gap-6">
            <div class="">
                <label for="nome_gruppo" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
                    {{ 'Name'|trans }}
                </label>
                <input type="text" name="nome_gruppo" id="nome_gruppo"
                       class="bg-white border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-primary-600 focus:border-primary-600 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-primary-500 dark:focus:border-primary-500"
                       placeholder="{{ 'Name'|trans }}" required>
            </div>

            <div class="">
                <!-- column for sending invite members -->
                <label for="utenti" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
                    {{ 'Invite members'|trans }}
                </label>



                <!-- input for email address with icon -->
                <div class="relative">
                    <input type="text"
                           name="utenti"
                           id="utenti"
                           class="bg-white border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-primary-600 focus:border-primary-600 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-primary-500 dark:focus:border-primary-500"
                           placeholder="{{ 'Email address'|trans }}" required>
                    <div class="absolute inset-y-0 right-0 flex items-center">
                        <button type="button"
                                class="inline-flex items-center px-4 py-2.5 text-sm font-medium text-center text-white bg-primary-700 rounded-lg focus:ring-4 focus:ring-primary-200 dark:focus:ring-primary-900 hover:bg-primary-800">
                            <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"
                                 xmlns="http://www.w3.org/2000/svg">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                      d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                            </svg>
                        </button>
                    </div>
                </div>

                <p class="mt-2 text-sm text-gray-500 dark:text-gray-400">
                    {{ 'Enter the email addresses of the people you want to invite to this group. They will receive an email with a link to join the group.'|trans }}
                </p>

            </div>

            <!-- table for showing members -->

            <div class="relative overflow-x-auto shadow-md sm:rounded-lg sm:col-span-2">
                <table class="w-full text-sm text-left text-gray-500 dark:text-gray-400">
                    <caption class="p-5 text-lg font-semibold text-left text-gray-900 bg-white dark:text-white dark:bg-gray-800">
                        {{ 'Members'|trans }}
                    </caption>
                    <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
                    <tr>
                        <th scope="col" class="px-6 py-3">
                            {{ 'Name'|trans }}
                        </th>
                        <th scope="col" class="px-6 py-3">
                            {{ 'Email'|trans }}
                        </th>
                        <th scope="col" class="px-6 py-3">
                            {{ 'Status'|trans }}
                        </th>

                        <th scope="col" class="px-6 py-3">
                            <span class="sr-only">Edit</span>
                        </th>
                    </tr>
                    </thead>
                    <tbody>
                    <tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700">
                        <th scope="row" class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap dark:text-white">
                            Jane Cooper
                        </th>
                        <td class="px-6 py-4">
                            <a href="#" class="text-sm text-gray-900 dark:text-gray-400">
                                jane.cooper@gmail.com
                            </a>
                        </td>

                        <td class="px-6 py-4 text-sm text-gray-500 whitespace-nowrap dark:text-gray-400">
                            <span class="px-2 py-1 font-semibold leading-tight text-green-700 bg-green-100 rounded-full dark:bg-green-700 dark:text-green-100">
                                {{ 'Active'|trans }}
                            </span>
                        </td>

                        <td class="px-6 py-4 text-right">
                            <a href="#" class="font-medium text-blue-600 dark:text-blue-500 hover:underline">Remove</a>
                        </td>
                    </tr>

                    </tbody>
                </table>
            </div>



        </div>


        <div class="fixed right-6 bottom-6 group ">
            <button type="submit"
                    class="inline-flex items-center px-5 py-2.5 mt-4 sm:mt-6 text-sm font-medium text-center text-white bg-primary-700 rounded-lg focus:ring-4 focus:ring-primary-200 dark:focus:ring-primary-900 hover:bg-primary-800">
                {{ 'Save'|trans }}
            </button>
        </div>


        <!--
        <div class="p-6 space-y-6">

            <div class="grid grid-cols-1 gap-6">
                <div>
                    <label for="nome_gruppo"
                           class="block text-sm font-medium text-gray-700 dark:text-gray-200">Nome</label>
                    <input type="text" name="nome_gruppo" id="nome_gruppo"
                           class="block w-full mt-1 rounded-lg shadow-sm dark:bg-gray-700 dark:border-gray-600 dark:text-gray-300 focus:border-primary-400 focus:ring focus:ring-primary-300 focus:ring-opacity-50">
                </div>
                <h3 class="text-lg font-semibold text-gray-900 dark:text-white">
                    Team
                </h3>
                <div x-data="sottoGruppiContainer"
                     data-utenti="{{ utenti|json_encode|e('html_attr') }}"
                     data-sottogruppi="{{ sottogruppi|json_encode|e('html_attr') }}">
                    <template x-for="(sottogruppo, index) in sottogruppi" :key="index">
                        <div class="mb-4 border rounded-lg p-4 space-y-2">

                            <div class="flex items-center space-x-4">
                                <label for="name"
                                       class="block text-sm font-medium text-gray-700 dark:text-gray-200">Nome</label>
                                <input type="text"
                                       x-model="sottogruppo.nome"
                                       :dataID="sottogruppo.id"
                                       :name="'sottogruppi[' + index + '][nome]'"
                                       :id="'nome-' + index"
                                       class="block w-full rounded-lg shadow-sm dark:bg-gray-700 dark:border-gray-600 dark:text-gray-300 focus:border-primary-400 focus:ring focus:ring-primary-300 focus:ring-opacity-50">
                            </div>

                            <div class="flex items-center space-x-4">
                                <label for="utenti"
                                       class="block text-sm font-medium text-gray-700 dark:text-gray-200">Utenti</label>
                                <select x-ref="select2" :id="'select2-' + index" x-data="utenti" id="utenti"
                                        x-bind:name="'sottogruppi[' + index + '][utenti][]'"
                                        x-model="sottogruppo.utenti"
                                        class="select2 block w-full mt-1 rounded-lg shadow-sm dark:bg-gray-700 dark:border-gray-600 dark:text-gray-300 focus:border-primary-400 focus:ring focus:ring-primary-300 focus:ring-opacity-50"
                                        multiple style="width: 100%">
                                    <template x-for="(utente, utenteIndex) in utenti" :key="utenteIndex">
                                        <option :value="utente.id" x-text="utente.nome"></option>
                                    </template>
                                </select>
                            </div>

                            <div class="text-right flex items-center space-x-2 justify-end">

                                <button type="button" @click="addSottoGruppo()"
                                        class="flex items-center justify-center text-white bg-green-500 hover:bg-green-600 focus:ring-4 focus:ring-green-300 font-medium rounded-lg text-sm px-4 py-2 dark:bg-green-500 dark:hover:bg-green-600 focus:outline-none dark:focus:ring-green-500">
                                    <ion-icon name="add-outline" class="h-3.5 w-3.5 mr-2"></ion-icon>
                                    {{ 'Add Team'|trans }}
                                </button>

                                <button type="button" @click="removeSottoGruppo(index)"
                                        class="flex items-center justify-center text-white bg-red-700 hover:bg-red-800 focus:ring-4 focus:ring-red-300 font-medium rounded-lg text-sm px-4 py-2 dark:bg-red-600 dark:hover:bg-red-700 focus:outline-none dark:focus:ring-red-800">
                                    <ion-icon name="trash-outline" class="h-3.5 w-3.5 mr-2"></ion-icon>
                                    <span>
                                                    {{ 'Remove Team'|trans }}
                                                </span>
                                </button>


                            </div>

                        </div>

                    </template>
                </div>


                <div>
                    <label for="status" class="block text-sm font-medium text-gray-700 dark:text-gray-200">Status</label>
                    <select id="status" name="status"
                            class="block w-full mt-1 rounded-lg shadow-sm dark:bg-gray-700 dark:border-gray-600 dark:text-gray-300 focus:border-primary-400 focus:ring focus:ring-primary-300 focus:ring-opacity-50">
                        <option value="1">Active</option>
                        <option value="0">Inactive</option>
                    </select>
                </div>
            </div>

        </div>

        <div class="absolute right-3 top-3 group  flex items-center">
            <button type="submit"
                    class="flex items-center justify-center text-white bg-green-500 hover:bg-green-600 focus:ring-4 focus:ring-green-300 font-medium rounded-lg text-sm px-4 py-2 dark:bg-green-500 dark:hover:bg-green-600 focus:outline-none dark:focus:ring-green-500 w-24 h-12">
                Save
            </button>

        </div>

        <div class="fixed right-6 bottom-6 group ">
            <button type="submit"  class="flex items-center justify-center text-white bg-green-500 hover:bg-green-600 focus:ring-4 focus:ring-green-300 font-medium rounded-lg text-sm px-4 py-2 dark:bg-green-500 dark:hover:bg-green-600 focus:outline-none dark:focus:ring-green-500">
                {{ 'Save'|trans }}
            </button>
        </div>

        -->
    </form>

{% endblock %}
